import { PracticaDirectivaDirective } from './practica-directiva.directive';

describe('PracticaDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new PracticaDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
